<%= link_to "Back", requests_path %>
<h1>
  Запит на вибірку з двома умовами через «and»
  Запит поверне всі транзакції, в яких об'єм заправленого палива більше 20 літрів, а загальна вартість менше 100 грн.
</h1>
<h2>
  <%= @sql %>
</h2>
<h2>
  Total Count
  <%= @records.count %>
</h2>
<table>
  <thead>
    <tr>
      <% @records.columns.each do |field| %>
        <th>
          <%= field %>
        </th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @records.each do |record| %>
      <tr>
        <% record.each do |_col, val| %>
          <td>
            <%= val %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
