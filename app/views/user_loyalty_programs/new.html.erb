<div class="centered-div">
  <h1>Add new user loyalty program</h1>
  <%= form_with model: UserLoyaltyProgram.new do |form| %>
    <% UserLoyaltyProgram.column_names.reject { |a| fields_to_exclude.include?(a)}.each do |field| %>
      <div class="inputs-wrapper">
        <%= form.label field.humanize, for: "user_loyalty_program_#{field.to_sym}" %>
        <%= form.text_field field.to_sym, required: true %>
      </div>
    <% end %>
    <div class="submit-buttons-wrapper">
      <%= link_to "Back", user_loyalty_programs_pages_path %>
      <%= form.submit "Save" %>
    </div>
  <% end %>
</div>
